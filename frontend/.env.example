# Optional: hard-code the backend base URL at build time.
# Web (served by the same nginx as the backend): leave EMPTY -> same origin.
# Android APK build: set to your public backend, e.g. https://dashboard.example.com
# (Users can also override this at runtime on the login screen.)
VITE_API_BASE=
